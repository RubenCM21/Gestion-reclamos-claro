# Frontend

Levantar el frontend localmente:

```bash
cd Gestion-reclamos-claro/frontend
python -m http.server 3000
```

Abrir en el navegador:

```text
http://localhost:3000
```

Para usar las APIs, el backend debe estar levantado en:

```text
http://localhost:8000
```
